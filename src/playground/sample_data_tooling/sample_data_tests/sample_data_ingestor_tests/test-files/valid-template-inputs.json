{
    "valid_json_shorthand": {
        "year": "year",
        "random number": "integer",
        "email": "email"
    },

    "valid_json_mapping": {
        "properties": {
            "first name":    { "type" : ["first_name"]},
            "last name":     { "type" : "last_name"},
            "companies":{ "type" : ["array", "company", 5] }
        }
    },

    "valid_json_array_shorthand": {
        "year": "year",
        "addresses": ["array", "address", "integer", [1, 3]],
        "director": "name"
    },

    "valid_json_array_mapping": {
        "properties": {
            "year":    { "type" : ["year"]},
            "addresses":     { "type" : ["array", "address", "integer", [1, 3]] },
            "director":{ "type" : "name" }
        }
    },

    "valid_json_array_mapping_args": {
        "properties": {
            "year":    { "type" : ["year"]},
            "integers":     { "type" : ["array", "integer", "integer", [1, 3], 5, 10] },
            "director":{ "type" : "name" }
        }
    },

    "valid_json_shorthand_data_trend": {
        "date": "unix_time",
        "average cpu usage": ["integer", 20, 30]
    }
}